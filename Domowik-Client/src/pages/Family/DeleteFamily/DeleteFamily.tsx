import { MdDeleteForever } from 'react-icons/md';

const DeleteFamily = () => {
  return (
    <a className="delete-family-btn" type="button">
      <MdDeleteForever size={50} color="#bf2015" />
    </a>
  );
};

export default DeleteFamily;
